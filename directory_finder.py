#This portion of code finds a target directory, and leaves a "marker file" there signifying success
#Additionally, it tabs-to-complete during the directory selection phase of the program
#if a directory is not available, it sends an error to the console
#marker file is called "foundit.txt" and contains the message 'Tallyho, directory_finder.py was here!'

import os
import sys
import glob

print("yeet!")
